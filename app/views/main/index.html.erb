<% if user_signed_in? %>
<nav class="navbar navbar-dark bg-primary navbar-expand-lg">
  <div class="container-fluid">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="<%= needs_path %>">Needs <span class="badge text-bg-secondary"><%= Need.all.count %></span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= matches_path %>">Matches <span class="badge text-bg-secondary"><%= Match.all.count %></span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= resources_path %>">Resources <span class="badge text-bg-secondary"><%= Resource.all.count %></span></a>
      </li>
    </ul>
  </div>
</nav>
<% if Organization.any? %>
Organization: <%= Organization.find(current_user.organization_id).name %>
<% else %>
Please create an organization!
<% end %>
<% else %>
<div class="alert alert-warning" role="alert">
  Please <a href="<%= new_session_path %>" class="alert-link">log in</a> to continue.
</div>
<% end %>