<p style="color: green"><%= notice %></p>

<h1>Resources</h1>

<div id="resources">
  <% @resources.each do |resource| %>
   <p> <%= render resource %>
    <%= link_to "Show", resource %></p>
  <% end %>
</div>

<%= link_to "New resource", new_resource_path %>
