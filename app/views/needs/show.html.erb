<%= render @need %>

<div>
  <%= link_to "Edit this need", edit_need_path(@need) %> |
  <%= link_to "Back to needs", needs_path %>

  <%= button_to "Delete this need", @need, method: :delete %>
</div>
